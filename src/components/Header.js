import React from 'react';
import './Header.css';

const Header = () => (
  <header className="header">
    <h1>Todo App</h1>
    <p>Add todo item and mark as completed or delete it when done.</p>
  </header>
);

export default Header;
